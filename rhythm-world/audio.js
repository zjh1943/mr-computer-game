import { ORIGINAL_THEME, notesForKind } from './music-score.js';

export function createAudioSystem(){let ctx,master,tempo=ORIGINAL_THEME.tempo,origin=0,loops=new Map();function unlock(){if(!ctx){ctx=new(AudioContext||webkitAudioContext)();master=ctx.createGain();master.gain.value=.5;master.connect(ctx.destination);origin=ctx.currentTime}ctx.resume();return ctx}function tone(freq,at,dur=.12,type='sine',gain=.08){const c=unlock(),o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.value=freq;o.connect(g);g.connect(master);g.gain.setValueAtTime(.001,at);g.gain.exponentialRampToValueAtTime(gain,at+.01);g.gain.exponentialRampToValueAtTime(.001,at+dur);o.start(at);o.stop(at+dur+.02)}function startLoop(id,kind='beat'){stopLoop(id);const beat=60/tempo,play=()=>{const c=unlock(),next=c.currentTime+.03;for(const note of notesForKind(kind)){const type=kind==='beat'?'square':kind==='voice'?'sawtooth':kind==='melody'?'triangle':kind==='bass'?'sawtooth':'sine';const duration=kind==='beat'?.12:kind==='melody'||kind==='chord'?.42:kind==='bass'?.3:.22;tone(note.frequency,next+note.beat*beat,duration,type,kind==='beat'?.065:kind==='bass'?.045:.032)}};play();loops.set(id,setInterval(play,beat*ORIGINAL_THEME.steps*1000))}function stopLoop(id){clearInterval(loops.get(id));loops.delete(id)}return{unlock,startLoop,stopLoop,stopAll(){[...loops.keys()].forEach(stopLoop)},startEnsemble(){['beat','bass','chord','melody','voice','whistle'].forEach(kind=>startLoop(`concert-${kind}`,kind))},setVolumes(v){unlock();master.gain.value=v},setMuted(){},setSolo(){},getBeatTime(){return ctx?(ctx.currentTime-origin)/(60/tempo):0}}}
