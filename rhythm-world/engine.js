import{createScene3D}from'./scene-3d.js';import{createCharacter3D,setCharacterMotion}from'./character-3d.js';import{createTown3D}from'./town-3d.js';import{createSky3D}from'./sky-3d.js';import{createThirdPersonCamera}from'./camera-3d.js';import{createInput3D}from'./input-3d.js';import{createInteraction3D}from'./interaction-3d.js';import{createNpcSystem}from'./npc-3d.js';import{getDayNightState,getWorldPhase}from'./day-night.js';import{distanceBetween}from'./interactions.js';
export const distanceTo=distanceBetween;
export function createEngine({canvas,state,onInteract,onMessage=()=>{}}){let mode='factory',player=createCharacter3D(state.player.character||'gray'),cameraControl,input,town,sky,npcs,interaction;player.position.set(state.player.x||0,0,state.player.z??state.player.y??0);player.rotation.y=Math.PI;function frame({now,dt}){const dayState=getDayNightState(getWorldPhase()),look=input.consumeLook();cameraControl.rotate(look.x,look.y);if(mode==='world'){const move=input.movement,speed=5.2,angle=cameraControl.yaw,dx=(move.x*Math.cos(angle)-move.y*Math.sin(angle))*speed*dt,dz=(-move.x*Math.sin(angle)-move.y*Math.cos(angle))*speed*dt;player.position.x+=dx;player.position.z+=dz;if(Math.hypot(dx,dz)>.001)player.rotation.y=Math.atan2(dx,dz);state.player.x=player.position.x;state.player.z=player.position.z;state.player.y=player.position.z;setCharacterMotion(player,{moving:Math.hypot(dx,dz)>.001,time:now,direction:player.rotation.y})}town.update(player.position);sky.update(dayState);npcs.update({time:now,dayState,player});cameraControl.update(dt);interaction.update()}
const runtime=createScene3D({canvas,onFrame:frame,onError:onMessage});runtime.scene.add(player);town=createTown3D({scene:runtime.scene,seed:state.seed});sky=createSky3D(runtime.scene,{onFarewell:onMessage});npcs=createNpcSystem({scene:runtime.scene,onSpeech:target=>{if(mode==='world')onInteract?.(target)}});input=createInput3D({canvas});cameraControl=createThirdPersonCamera({camera:runtime.camera,canvas,target:player});interaction=createInteraction3D({camera:runtime.camera,canvas,getTargets:()=>[...town.getInteractables(),...npcs.getInteractables()],getPlayer:()=>player,onInteract});town.update(player.position);return{start:()=>runtime.start(),stop:()=>runtime.stop(),resize:()=>runtime.resize(),setMode(v){mode=v},setJoystick(x,y){input.setVirtual(x,y)},setPlayerPosition(x,z){player.position.set(x,0,z);state.player.x=x;state.player.y=z;state.player.z=z;town.update(player.position)},setPlayerCharacter(id){const p=player.position.clone(),r=player.rotation.y;runtime.scene.remove(player);player=createCharacter3D(id);player.position.copy(p);player.rotation.y=r;runtime.scene.add(player);cameraControl.setTarget(player);state.player.character=id},getNearbyInteractable(){if(mode==='factory')return{kind:'factoryExit',label:'离开生产中心'};return interaction.currentTarget},getMode:()=>mode,residentHomes:npcs.entities.map(n=>n.home),isGoingHome:()=>getDayNightState(getWorldPhase()).isNight,dispose(){interaction.dispose();input.dispose();npcs.dispose();sky.dispose();town.dispose();runtime.dispose()}}
}
