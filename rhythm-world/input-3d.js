export function createInput3D({canvas,blockedSelectors='#hud,.modal,.joystick,.interact'}){const keys=new Set();let lookX=0,lookY=0,virtual={x:0,y:0},drag=null;const down=e=>keys.add(e.code),up=e=>keys.delete(e.code);addEventListener('keydown',down);addEventListener('keyup',up);canvas.addEventListener('pointerdown',e=>{if(e.target.closest?.(blockedSelectors))return;drag={id:e.pointerId,x:e.clientX,y:e.clientY};canvas.setPointerCapture?.(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(!drag||drag.id!==e.pointerId)return;lookX+=e.clientX-drag.x;lookY+=e.clientY-drag.y;drag.x=e.clientX;drag.y=e.clientY});canvas.addEventListener('pointerup',()=>drag=null);return{get movement(){let x=(keys.has('ArrowRight')||keys.has('KeyD')?1:0)-(keys.has('ArrowLeft')||keys.has('KeyA')?1:0)+virtual.x,y=(keys.has('ArrowUp')||keys.has('KeyW')?1:0)-(keys.has('ArrowDown')||keys.has('KeyS')?1:0)-virtual.y;const m=Math.hypot(x,y)||1;return{x:x/m,y:y/m}},consumeLook(){const out={x:lookX,y:lookY};lookX=lookY=0;return out},setVirtual(x,y){virtual={x,y}},dispose(){removeEventListener('keydown',down);removeEventListener('keyup',up)}}}
